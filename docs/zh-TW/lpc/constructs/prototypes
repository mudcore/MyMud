* 函式的原型 (prototype):

LPC 得函式原型與 ANSI C 非常相似. 函式原型可以做較好的型態檢查, 並當作一
種「前宣告」 ('forward' declaration).

傳回值形態 函式名稱( 第一個參數型態 參數一, 第二個參數形態 參數二, ... );

上面的函式在函式原型中, 參數不需要給名稱:

傳回值形態 函式名稱( 參數一, 參數二, ... );

翻譯:	Spock @ FF	97.Aug.10.
