名稱：
	all_previous_objects - 傳回呼叫目前函式所有的物件陣列。
語法：
	object array all_previous_objects();
	
	物件 陣列 all_previous_objects();
用法：
	傳回一個呼叫目前函式所有的物件陣列。 
	注意，區域函式呼叫不會把 previous_object() 設定為目前的物件，它不會
	變。
	
	陣列的第一個元素是 previous_object()，接著是 previous_object(1)，以
	此類推。
參考：
	call_other, origin, previous_object
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.Aug.28.	v22 版
