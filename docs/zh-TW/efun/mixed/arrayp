名稱：
	arrayp - 判斷一個指定的變數是否為陣列 (array)。
語法：
	int arrayp( mixed arg );

	整數 arrayp( 混合 參數 );
用法：
	如果「arg」是一個陣列，就傳回 1。不是則傳回 0。
參考：
	typeof
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.Aug.23.	v22 版
