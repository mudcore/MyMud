名稱：
	bufferp - 判斷一個指定的變數是否為一個暫存區 (buffer)。
語法：
	int bufferp( mixed arg );

	整數 bufferp( 混合 參數 );
用法：
	如果「arg」是一個暫存區值就傳回 1，不是就傳回 0。
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.Aug.23.	v22 版
