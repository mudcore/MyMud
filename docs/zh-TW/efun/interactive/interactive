名稱：
	interactive - 判斷一個指定的物件是否為「互動」(interactive)。
語法：
	int interactive( object ob );

	整數 interactive( 物件 物件 );
用法：
	如果 ob 是一個互動玩家，就傳回非零值。如果玩家斷線 (link dead)，則
	傳回 0。
參考：
	query_ip_number, query_ip_name, enable_commands
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.Jun.26.	v22 版
