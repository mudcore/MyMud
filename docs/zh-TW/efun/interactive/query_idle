名稱：
	query_idle - 詢問一個互動 (interactive) 玩家已經發呆 (idle) 幾秒。
語法：
	int query_idle( object ob );

	整數 query_idle( 物件 物件 );
用法：
	詢問一個互動 (interactive) 玩家物件已經發呆 (idle) 幾秒。
參考：
	in_edit, in_input
作者：
	Tim Hollebeek  Beek@ZorkMUD, Lima Bean, IdeaExchange, and elsewhere
翻譯：
	spock@muds.net		2000.Jul.1.	v22 版
